import * as React from "react";

interface ITableNode {
  data: Record<string, any>;
}

const TableNode: React.FC<ITableNode> = props => {
  return <div>Table React</div>;
};

export default TableNode;
